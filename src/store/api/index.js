// import axios from 'axios'
// import {baseParam} from '../getters.js'

export default {
  getCartNum(cb) {
    // axios.get('flow.php', {
    //   params: Object.assign(baseParam(), {step: 'cart'})
    // })
    // .then(({data: {data, errcode, msg}}) => {
    //   // console.log(data)
    //   if (errcode === 0) {
    //     // 获取购物车数据,回调给调用者
    //     cb(data)
    //   } else {
    //     console.error('获取购物车失败:' + msg)
    //   }
    // }, (response) => {
    //   // error callback
    //   console.error(response)
    // })
  }
}
